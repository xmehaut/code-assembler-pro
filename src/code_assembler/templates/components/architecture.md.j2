## {{ emoji.building }} Architecture Analysis

**Main Components:**
{% for comp in components %}
- `{{ comp.name }}/` ({{ comp.count }} file{{ 's' if comp.count > 1 }})
{% endfor %}

**File Distribution by Type:**
{% for item in distribution %}
- `{{ item.ext }}` ({{ item.lang }}): {{ item.count }} file{{ 's' if item.count > 1 }} ({{ item.percentage }}%)
{% endfor %}

**Detected Design Patterns:**
{% for pattern in patterns %}
- {{ pattern }}
{% endfor %}

**Structure:**
- Maximum Depth: {{ max_depth }} level{{ 's' if max_depth > 1 }}
- Distribution by Depth:
{% for depth, count in depth_distribution.items() %}
  - Level {{ depth }}: {{ count }} file{{ 's' if count > 1 }}
{% endfor %}